# Copied to the CIRCUITPY drive. CircuitPython reads this on boot.

SAMPLE_INTERVAL_S = 60
USB_SAMPLE_INTERVAL_S = 60
SENSOR_WARMUP_S = 5
MAX_STALE_REFRESH_S = 3600
SENSOR_SAMPLES = 3
KEEP_SENSOR_ON_BELOW_S = 30
LOW_BATTERY_V = 3.4
LOW_BATTERY_SLEEP_S = 3600
# deep = lowest power, restarts firmware each wake
# light = RAM kept, faster wake, higher sleep current
# no = CPU always on, LDO2/sensor only during each sample
# full = CPU and LDO2/sensor always on
SLEEP_MODE = "light"
VOLTAGE_LOG = 1
# Logs go to CIRCUITPY/data/voltN.csv. Hold A at reset to copy firmware.
