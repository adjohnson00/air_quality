# Copied to the CIRCUITPY drive. CircuitPython reads this on boot.

SAMPLE_INTERVAL_S = 900
USB_SAMPLE_INTERVAL_S = 180
SENSOR_WARMUP_S = 15
PM25_REFRESH_DELTA = 2
MAX_STALE_REFRESH_S = 3600
SENSOR_SAMPLES = 3
